# 16 综合实战：全能旅行助手项目（全章收官）

前面十五节，我们把 LangGraph 的零件一个个拆开学了一遍：状态图、条件路由、Send 并行、流式调试、Checkpointer、Store 记忆、RetryPolicy、子图、interrupt、Functional API……这一节是全章的**装机现场**：把这些零件全部装进一个真实可运行的开源级项目——**智能旅行管家系统（Trip Assistant）**，看看它们在企业级代码里各自长什么样、拼在哪里。

> **💡 代码位置指引**：
> 本实战的完整代码在本章 `code/travel_agent_v2/` 目录下（含 [README](code/travel_agent_v2/README.md)、`requirements.txt` 与 `.env.example`）。代码已安全脱敏，采用企业级模块化重构，适配 LangChain / LangGraph 1.x 标准接口。此外，02~14 节每节的最小可运行示例都放在 `code/examples/` 中（无需 API Key 即可运行），可与本节对照着跑。

## 1. 项目全貌（它能干什么？）

这个项目模拟了一个真实旅游平台的客服场景：一个 SQLite 本地库存放航班、酒店、租车和旅游路线的虚拟数据。用户只需一句：

> “帮我把下周二飞东京的航班退了，改签到周三，顺便在机场附近订一辆带儿童座椅的SUV。”

整个 Multi-Agent 体系会自动完成：
1. **主助理**识别出多个意图（09 节的 Routing 模式）。
2. 路由到**航班助理**，查询退改签政策（`lookup_policy` 向量检索工具），尝试调用 `update_ticket_to_new_flight`。
3. LangGraph 发现这是敏感操作，**挂起线程**，等待用户在终端输入 `y` 确认（06 节的 Human-in-the-loop）。
4. 确认后完成改签，航班助理调用 `CompleteOrEscalate` 退回主助理（07 节的状态栈弹栈）。
5. **主助理**接着路由到**租车助理**，搜索周三的 SUV。
6. 走完整个流转后，主助理统一回复用户（05 节的流式输出全程可见）。

## 2. 核心代码结构剖析

进入 `code/travel_agent_v2`，你会看到以下核心文件，这是一个高度解耦、可扩展的企业级 Agent 工程目录结构：

- 📁 `core/`（大脑图谱层与状态管理）
  - `state.py`：定义了 `State` 和 `dialog_state` 状态栈。它是整个项目的血液。
  - `llm_config.py`：大模型的统一初始化入口（通过 `.env` 环境变量切换 GPT / DeepSeek / Claude 兼容服务）。
  - `primary_agent.py`：主助理（大堂经理）的 Prompt 和路由逻辑。
  - `sub_agents.py`：四个子助理（机票、酒店、租车、游览）的专有 Prompt 与工具池。
  - `graph_builder.py`：子工作流拼装厂，定义了每个子助理“安全工具 / 敏感工具”的不同连线逻辑。
  - `models.py`：Pydantic 数据模型，包括路由转交意图工具。
- 📁 `tools/`（手脚工具层）
  - `flights.py`、`hotels.py` 等：用 `@tool` 装饰器编写的真实 Python 函数，操作 SQLite 数据库。
  - `tool_handler.py`：工具的统一容错封装（08 节讲的 `with_fallbacks` 机制）。
  - `retriever_vector.py`：航司政策 FAQ 的向量检索（`lookup_policy` 工具）。
- 📁 `db/`（虚拟数据库层）：`travel*.sqlite` 测试数据库。
- `main.py`：主运行文件，把主助理与四个子助理拼成总 `StateGraph`，挂载 Checkpointer 并启动终端对话。

## 3. 知识对号入座：每个零件是哪一节学的？

这台“整机”的每个部件，都对应全章的一个知识点。读懂这张表，就等于把全章复习了一遍：

| 项目里的部件 | 用到的知识点 | 对应小节 |
| :--- | :--- | :--- |
| `State` + `dialog_state` 状态栈 | TypedDict 状态、自定义 reducer | 02 / 07 |
| 主助理的四个“转交工具” | Pydantic 意图模型 + 条件路由 | 03 / 07 |
| 每个子助理内部的 `tools_condition` + 工具节点 | 工具调用循环（ReAct 闭环） | 08 |
| `create_tool_node_with_fallback` | ToolNode 容错（`with_fallbacks`） | 08 |
| 安全工具 / 敏感工具分池 + 分池路由 | Routing 模式 | 03 / 09 |
| 主助理 → 子助理的委派架构 | Supervisor 型 Multi-Agent（平铺实现） | 07 / 12 |
| `interrupt_before` 敏感操作拦截 | Human-in-the-loop | 06 / 13 |
| `MemorySaver` + `thread_id` | Checkpointer 短期记忆 | 06 / 10 |
| `main.py` 的 `graph.stream(..., stream_mode='values')` | 流式调试 | 05 |
| `update_dates()` 数据库时间平移 | 工程细节：让“明天”永远可查 | 本节 |

> 💡 **对照着改造练手**：学完 13 节后，可以把项目里的静态 `interrupt_before` 改造成节点内动态 `interrupt()`——只拦截真正需要审批的操作；学完 12 节后，可以试着把某个子助理真正拆成独立子图。这两个改造就是最好的毕业考试。

## 4. 核心高光代码赏析

### 4.1 严格的敏感工具拦截

在 `core/graph_builder.py` 中，开发者将每个子助理的工具分为了两拨：

```python
# 航班子助理的工具池被切分成两半
update_flight_safe_tools = [search_flights] # 安全工具：查询
update_flight_sensitive_tools = [update_ticket_to_new_flight, cancel_ticket] # 敏感工具：退改签

# 在图中添加两个不同的节点
builder.add_node("update_flight_safe_tools", create_tool_node_with_fallback(update_flight_safe_tools))
builder.add_node("update_flight_sensitive_tools", create_tool_node_with_fallback(update_flight_sensitive_tools))
```

在总运行文件 `main.py` 中，通过 `interrupt_before` 设卡拦截：

```python
memory = MemorySaver()
graph = builder.compile(
    checkpointer=memory,
    # 只要路由指向以下这四个节点，统统踩刹车挂起，等待用户同意！
    interrupt_before=[
        "update_flight_sensitive_tools",
        "book_car_rental_sensitive_tools",
        "book_hotel_sensitive_tools",
        "book_excursion_sensitive_tools",
    ]
)
```

### 4.2 对话状态压栈与弹栈

在 `core/state.py` 中，`dialog_state` 决定了当前是谁在跟用户对话：

```python
def update_dialog_stack(left: list[str], right: Optional[str]) -> list[str]:
    if right is None:
        return left
    if right == "pop":
        return left[:-1]  # 弹栈：子助理说“我搞定了，还给主助理”，状态回退
    return left + [right] # 压栈：主助理说“去机票部”，栈里多了一个 update_flight
```

## 5. 如何运行？

要亲自感受这个强大的系统，请进入代码目录 `code/travel_agent_v2/`（该目录自带 [README](code/travel_agent_v2/README.md)，含目录详解与模型切换说明）：

1. **安装依赖**（依赖清单见项目根目录的 `requirements.txt`）：
   ```bash
   pip install -r requirements.txt
   ```
2. **配置环境变量**（项目根目录提供了 `.env.example` 模板，复制为 `.env` 后填入你的 Key；`core/llm_config.py` 会在启动时自动加载它）：
   ```bash
   cp .env.example .env
   ```
   需要配置的核心变量：
   ```bash
   OPENAI_API_KEY="sk-xxxx"
   OPENAI_MODEL_NAME="gpt-4o-mini"          # 模型名与下面的服务地址必须配套！
   OPENAI_API_BASE="https://api.openai.com/v1"
   TAVILY_API_KEY="tvly-xxxx"               # 可选：主助理的全网搜索工具
   ```
   > ⚠️ **模型名与服务地址必须配套**：如果你想用 Claude / DeepSeek 等兼容 OpenAI 协议的模型，请同时修改 `OPENAI_MODEL_NAME` 和 `OPENAI_API_BASE`（`.env.example` 里给了三套可直接抄的方案）。给 OpenAI 官方地址配一个 `claude-*` 模型名是跑不通的。
3. **初始化数据库时间**：
   （为了防止提示词中的“明天”搜不到老数据库里的虚拟航班，脚本会自动平移数据库时间）
   代码启动时 `main.py` 会自动调用 `update_dates()` 函数刷新本地 SQLite 库。
4. **启动终端对话**（在 `travel_agent_v2/` 目录内运行）：
   ```bash
   python main.py
   ```

接下来，你就可以像使唤真实客服一样，开始刁难它吧！通过终端打印的绿色事件流，你能清晰地看到大模型是如何在不同的节点中穿梭、交接、并遇到敏感动作主动停下来向你请示的。

## 6. 全章收官：从一张图到一个平台

从 01 节的“飞行棋棋盘”开始，这一章我们走完了 LangGraph 的完整版图：

- **基础轨道**（01-05）：State、节点、条件路由、Send 并行、可视化与流式；
- **控制与记忆**（06、10、11、13）：Checkpointer、Store 长期记忆、Time Travel、持久执行、interrupt 动态中断；
- **智能体形态**（07、08、12）：工具调用循环、分层 Multi-Agent、真子图与编队全谱；
- **方法论与工程化**（09、14、15、16）：设计模式选型、Functional API、部署与观测、综合实战。

如果你只记一句话，请记住这句贯穿全章的核心思想：**程序员设计严谨的轨道，大模型只在关键路口做决策。** 可控性，就是 LangGraph 相对于“一把梭 Agent”的全部价值。

> **小结：**
> 从早期的 LangChain Agent 一把梭，到今天的 LangGraph 1.x 意图流架构编排。我们看到 AI 编程正在走向真正的工程化。控制权不再完全听天由命地交给 LLM 去瞎猜，而是**由程序员设计好严谨的轨道图，大模型只负责在十字路口做路由判断**。这就是 Vibe Coding 时代的最佳 Agent 开发实践模式！

## 7. 扩展阅读

**官方文档**
- LangGraph 快速开始（把全章知识串成一条官方主线）：[docs.langchain.com/oss/python/langgraph/quickstart](https://docs.langchain.com/oss/python/langgraph/quickstart)
- 应用工程结构（生产级项目的目录组织参考，与本项目的 `core/` + `tools/` 分层同思路）：[docs.langchain.com/oss/python/langgraph/application-structure](https://docs.langchain.com/oss/python/langgraph/application-structure)
- LangGraph 官方仓库（本文项目的多助理路由模式参考了官方 customer-support 经典案例的设计）：[github.com/langchain-ai/langgraph](https://github.com/langchain-ai/langgraph)
