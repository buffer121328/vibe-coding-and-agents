"""基础设施层：日志、模型、业务库连接、一次性数据脚本。

这一层是**最底下那层**：谁都可以用它，但它不认识上面的任何东西——
不 import agent / tools / web，所以怎么换都不会牵动业务逻辑。
放到这里的判据很简单：**换个环境就得换的东西**（日志落哪、模型走哪个网关、
库文件在哪、数据怎么灌），以及只在开发/部署时跑一次、运行期不参与的脚本（`scripts/`）。
"""
