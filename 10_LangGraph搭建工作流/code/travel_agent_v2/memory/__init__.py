"""记忆层：跨会话的长期记忆（LangGraph Store）。

管什么：这台机器「记性」的那一半——会员档案存在哪、一人一个抽屉怎么分、重启后还在不在。

另一半分在别处，别混：
- **单条对话的存档**（Checkpointer）挂在图上，由 `web.runtime.hub` 装配；
- **会话目录**（谁有几条对话、各叫什么）是应用库里的业务表，在 `web/sessions.py`。

本层只有一个实现文件 `store.py`：换 Postgres 也是换它一个，工具那边一行不用动。
"""
