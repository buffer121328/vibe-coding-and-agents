"""展示层：把数据摆成给人看的样子。

管什么：页面骨架（Air Tags + 全部 CSS）、城市几何标记与航图 SVG、城市中英文名、
工具回执 → 卡片、以及「不经过模型直接翻库存」的浏览查询。

这里的东西**都是纯的**：只接参数、吐 HTML / SVG / dict，不碰会话、不认识 Desk、
不 import `web.runtime`。所以 `web.runtime` 可以反向依赖它，而它永远不会反向依赖运行时。
"""
